{
  "name": "otherworld-reads-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The one piece of backend Otherworld Reads needs: a Cloudflare Worker holding the Anthropic key.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
